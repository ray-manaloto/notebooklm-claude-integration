# ============================================================================
# MCP Server Environment Variables
# ============================================================================
#
# Add these to your ~/.zshrc, ~/.bashrc, or ~/.profile
# Then run: source ~/.zshrc (or appropriate shell config)
#
# SECURITY NOTE:
#   - Never commit this file with real credentials
#   - Use a password manager for sensitive tokens
#   - Rotate tokens regularly
# ============================================================================

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org, user
export GITHUB_TOKEN="ghp_your_token_here"

# Brave Search API Key
# Get from: https://brave.com/search/api/
export BRAVE_API_KEY="BSA_your_key_here"

# OpenAI API Key (if using GPT-based MCP servers)
export OPENAI_API_KEY="sk-your_key_here"

# Anthropic API Key (if using Claude API in tools)
export ANTHROPIC_API_KEY="sk-ant-your_key_here"

# Perplexity API Key (if using Perplexity MCP)
export PERPLEXITY_API_KEY="pplx-your_key_here"

# Optional: NotebookLM session data (set by notebooklm-mcp automatically)
# export NOTEBOOKLM_SESSION="auto_generated_by_mcp"

# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file to a safe location (not in git!)
#    cp env.example ~/.mcp-env
#
# 2. Edit with your actual credentials
#    nano ~/.mcp-env
#
# 3. Source it in your shell config
#    echo "source ~/.mcp-env" >> ~/.zshrc
#
# 4. Reload your shell
#    source ~/.zshrc
#
# 5. Verify variables are set
#    echo $GITHUB_TOKEN
#
# ============================================================================
